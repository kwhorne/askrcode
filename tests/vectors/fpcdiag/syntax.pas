{ A fixture whose first error is fatal, so the compiler stops there. This is
  the other shape: a Fatal with a position, and no summary line. }
unit syntax;
{$mode Delphi}{$H+}
interface

implementation

procedure P;
begin
  WriteLn('x';
end;

end.
